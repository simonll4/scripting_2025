# Verbosity of the program; available values are "error", "warn", "info", "debug".
logLevel: info
# Destinations of log messages; available values are "stdout", "file" and "syslog".
logDestinations: [stdout]

# Enable downloading recordings from the playback server.
playback: yes
# Address of the playback server listener.
playbackAddress: :9996
# Value of the Access-Control-Allow-Origin header provided in every HTTP response.
playbackAllowOrigin: '*'

rtsp: yes
# List of enabled RTSP transport protocols.
# UDP is the most performant, but doesn't work when there's a NAT/firewall between
# server and clients.
# UDP-multicast allows to save bandwidth when clients are all in the same LAN.
# TCP is the most versatile.
# The handshake is always performed with TCP.
rtspTransports: [tcp]
# Use secure protocol variants (RTSPS, TLS, SRTP).
# Available values are "no", "strict", "optional".
rtspEncryption: "no"
# Address of the TCP/RTSP listener. This is needed only when encryption is "no" or "optional".
rtspAddress: :8554

paths:
  all:
    record: yes
    recordFormat: fmp4
    # Estructura de carpetas con timestamps en el nombre del archivo
    recordPath: /recordings/%path/%Y/%m/%d/%H/%M/%S-%f
    # Segmentos de 1 hora (ajusta según tu caso de uso)
    recordSegmentDuration: 1h
    # Opcional: retención automática (ej: borrar después de 7 días)
    # recordDeleteAfter: 168h
